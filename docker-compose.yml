version: "3.8"
services:
   app:
      build:
         context: .
         dockerfile: Dockerfile
      container_name: my-portfolio-app
      ports:
         - "8080:8080"
      env_file:
         - config/.env.production
      restart: always
      # Uncomment the volumes below for local development (bind-mount source to container)
      # volumes:
      #   - .:/usr/src/app
      #   - /usr/src/app/node_modules
